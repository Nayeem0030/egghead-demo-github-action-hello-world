name: Hello World 

on:
  push:
    branches: [ "master" ] 
  pull_request:
    branches: [ "master" ]


jobs:
  hello-world :
    runs-on: Ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: ./action1.yml/
